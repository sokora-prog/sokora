FROM node:20-alpine AS builder

WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .

ARG VITE_API_URL=/api
ENV VITE_API_URL=$VITE_API_URL

RUN npm run build

# ── Image finale : copie uniquement le dist ──
FROM alpine:3.18
WORKDIR /dist
COPY --from=builder /app/dist .
